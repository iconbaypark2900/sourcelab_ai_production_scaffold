"""Local v1 release manifest and checklist for SourceLab AI.

Instruction:
- Provides schemas, manifest builder, and checklist for local v1 release candidate.
- Used by `sourcelab release check`, `sourcelab local-demo`, and strict release verification.
"""

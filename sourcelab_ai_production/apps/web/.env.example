# Base URL of the SourceLab FastAPI backend.
#
# Start the backend from the project root with:
#   source .venv/bin/activate && sourcelab api --serve
#
# Copy this file to .env.local and adjust if your backend runs elsewhere.
NEXT_PUBLIC_SOURCELAB_API_URL=http://127.0.0.1:8000
